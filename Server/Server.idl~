moudle Server
{
	interface Hello
	{
		boolean open(in string filename);
		string request(in string filename);
		boolean modifyRecord(in int record, in string newRecord)
		boolean modifyFile(in string newFile)

		boolean changeActiveFile(in string newFileName)
	};
};
